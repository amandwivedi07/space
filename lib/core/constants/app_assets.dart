/// Bundled brand artwork. Regenerate the source files from the master lockup
/// (see `Space_talk/brand/`) rather than editing these in place.
class AppAssets {
  const AppAssets._();

  /// The mark alone, transparent — reads on both paper and night.
  static const String logoMark = 'assets/brand/logo-mark.png';

  /// Mark plus wordmark, on its own dark plate.
  static const String logoLockup = 'assets/brand/logo-lockup.png';
}
