<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Android 13+ will not display a notification from an app that has not
         declared this and been granted it at runtime. Without it FCM still
         delivers the message and the app still wakes, so the logs look
         healthy — nothing is ever drawn on screen. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Stripped from the merged manifest. The stt_record plugin declares
         these for its optional "recording in progress" notification, which we
         never switch on: its own start() returns immediately unless
         enableSystemNotification is passed, and we never pass it. Left in, they
         make Play demand a video demonstrating a foreground microphone feature
         the app does not have. Recording happens only while the room is open
         and on screen. If that ever changes — recording that continues in the
         background — these have to come back, and Play will need that video. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
        tools:node="remove"/>
    <application
        android:label="Spacechat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Where FCM posts a card notification when the payload names no
             channel, or names one that does not exist yet. Without this the
             SDK invents a low-importance "Miscellaneous" channel and the
             banner never appears. MainActivity creates the channel itself. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="space_talk_cards" />

        <!-- The service that would use those permissions. Removed with them so
             the component cannot be started by anything at all. -->
        <service
            android:name="vn.fighttechvn.stt_record.SttRecordForegroundService"
            tools:node="remove"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
